# StatusServiceApplication
The Status Service is a REST-based microservice responsible for fetching and delivering role-based status configurations for the YONO application.  It retrieves active status records mapped to a given user role from Oracle database tables and returns the data in a structured API response format.
